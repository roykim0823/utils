install google\_test from github.
	make install
install pkgconfig
	sudo apt-get install pkg-config
add the following in .bashrc
	export PKG_CONFIG_PATH=/usr/local/lib64/pkgconfig
make install -> /usr/local/google\_test
